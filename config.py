# === Konstanten ===
GRID_SIZE = 10
GRID_WIDTH = 100 # X (spalte)
GRID_HEIGHT = 100 # Y (zeile)
SCREEN_WIDTH = GRID_WIDTH * GRID_SIZE
SCREEN_HEIGHT = GRID_HEIGHT * GRID_SIZE
MONTE_CARLO_FILE = "Monte-Carlo-Methode.csv"
Q_LEARNING_FILE = "Q-Learning.csv"
DIRECTIONS = ['up', 'down', 'left', 'right']

# Farben
WHITE = (255, 255, 255)
GREEN = (0, 255, 0)
RED = (255, 0, 0)